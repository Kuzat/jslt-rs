{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build jslt vscode extension",
      "detail": "Build the language server binary and compile the extension TypeScript.",
      "type": "shell",
      "command": "bash scripts/build.sh release && npx tsc -p ./",
      "options": {
        "cwd": "${workspaceFolder}/editors/vscode"
      },
      "group": "build",
      "problemMatcher": ["$tsc", "$rustc"]
    }
  ]
}
